package frc.robot.subsystems.base;

/**
 * This enum is used in the getGameColor method in the ColorSensor2021 class; it's task is
 * to only allow these values to return when calling the method.
 */
public enum Color2021
{
    RED, GREEN, CYAN, YELLOW, UNKNOWN, NONE
};
